# scena 1.10.5 release review

This patch release exposes the existing clipping-plane renderer capability as a
typed, versioned SceneHost contract for native and browser consumers. It is
accepted only when the focused contract proof, isolated remote release
rehearsal, public workflow matrix, tag, and published-version checks agree on
the same source commit.
