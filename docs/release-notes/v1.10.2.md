# scena 1.10.2

Release-candidate repair and validation follow-up.

- Restore renderer-owned WebGL2 canvas readback after the surface has been
  presented, so CPU/GPU browser parity uses the visible frame.
- Keep the final hero recipe's reflective floor aligned with the photographic
  contract and document the browser headless control used by browser proofs.
- Strengthen cross-backend double-sided material proof by comparing rendered
  RGB output with an empty GPU frame while ignoring backend-specific alpha clear.
